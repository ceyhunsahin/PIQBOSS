import fs from 'node:fs';

const path = 'D:/PIQSOFT/piqoff/plugins/queries/mobileDetailQueries.json';
const q = JSON.parse(fs.readFileSync(path, 'utf8'));

function add(id, query, param)
{
  q[`BOSS_POS_${id}`] = { query, param };
}

add('quickMonthFavorite', `SELECT TOP 5 ITEM_NAME, ITEM_CODE, SUM(CASE WHEN TYPE = 0 THEN QUANTITY ELSE QUANTITY * -1 END) AS QTY, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) AS MARGIN_HT, CASE WHEN SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END) <> 0 THEN SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) / NULLIF(SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END), 0) * 100 ELSE 0 END AS MARGIN_PERCENT FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 GROUP BY ITEM_NAME, ITEM_CODE ORDER BY MARGIN_PERCENT DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickWeekFavorite', `SELECT TOP 5 ITEM_NAME, ITEM_CODE, SUM(CASE WHEN TYPE = 0 THEN QUANTITY ELSE QUANTITY * -1 END) AS QTY, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) AS MARGIN_HT, CASE WHEN SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END) <> 0 THEN SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) / NULLIF(SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END), 0) * 100 ELSE 0 END AS MARGIN_PERCENT FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 GROUP BY ITEM_NAME, ITEM_CODE ORDER BY MARGIN_PERCENT DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickRangePromoTopProduct', `SELECT TOP 5 ITEM_NAME, ITEM_CODE, COUNT(*) AS CNT, SUM(DISCOUNT) AS DISCOUNT_TOTAL FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 AND (PROMO_TYPE IN (1,5) OR DISCOUNT > 0) GROUP BY ITEM_NAME, ITEM_CODE ORDER BY CNT DESC, DISCOUNT_TOTAL DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickRangeBestGroup', `SELECT TOP 5 ITEM_GRP_NAME, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END) AS TOTAL_CA FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 AND ITEM_GRP_NAME IS NOT NULL GROUP BY ITEM_GRP_NAME ORDER BY TOTAL_CA DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickDiscountMonth', `SELECT SUM(CASE WHEN TYPE = 0 THEN DISCOUNT ELSE DISCOUNT * -1 END) AS DISCOUNT_TOTAL, COUNT(DISTINCT GUID) AS TICKET_COUNT FROM POS_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 AND DISCOUNT <> 0`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickLoyaltyMonth', `SELECT SUM(CASE WHEN TYPE = 0 THEN LOYALTY ELSE LOYALTY * -1 END) AS LOYALTY_TOTAL, COUNT(DISTINCT GUID) AS TICKET_COUNT FROM POS_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 AND LOYALTY <> 0`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickMonthCa', `SELECT SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END) AS TOTAL_CA, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) AS MARGIN_HT, CASE WHEN SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END) <> 0 THEN SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) / NULLIF(SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END), 0) * 100 ELSE 0 END AS MARGIN_PERCENT FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickLastYearMonthCa', `SELECT SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END) AS TOTAL_CA, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) AS MARGIN_HT, CASE WHEN SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END) <> 0 THEN SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) / NULLIF(SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END), 0) * 100 ELSE 0 END AS MARGIN_PERCENT FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickBestMonthCa', `SELECT TOP 5 YEAR(DOC_DATE) AS SALE_YEAR, MONTH(DOC_DATE) AS SALE_MONTH, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END) AS TOTAL_CA FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 GROUP BY YEAR(DOC_DATE), MONTH(DOC_DATE) ORDER BY TOTAL_CA DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickBestWeekCa', `SELECT TOP 5 DATEPART(ISO_WEEK, DOC_DATE) AS SALE_WEEK, MIN(DOC_DATE) AS WEEK_START, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END) AS TOTAL_CA FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 GROUP BY DATEPART(ISO_WEEK, DOC_DATE) ORDER BY TOTAL_CA DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickBestDayMonthCa', `SELECT TOP 5 DOC_DATE, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END) AS TOTAL_CA FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 GROUP BY DOC_DATE ORDER BY TOTAL_CA DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickBestDayWeekCa', `SELECT TOP 5 DOC_DATE, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END) AS TOTAL_CA FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 GROUP BY DOC_DATE ORDER BY TOTAL_CA DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickBestMonthMargin', `SELECT TOP 5 YEAR(DOC_DATE) AS SALE_YEAR, MONTH(DOC_DATE) AS SALE_MONTH, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) AS MARGIN_HT, CASE WHEN SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END) <> 0 THEN SUM(CASE WHEN TYPE = 0 THEN FAMOUNT - (COST_PRICE * QUANTITY) ELSE (FAMOUNT - (COST_PRICE * QUANTITY)) * -1 END) / NULLIF(SUM(CASE WHEN TYPE = 0 THEN COST_PRICE * QUANTITY ELSE (COST_PRICE * QUANTITY) * -1 END), 0) * 100 ELSE 0 END AS MARGIN_PERCENT FROM POS_SALE_DATEIL_REPORT_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 GROUP BY YEAR(DOC_DATE), MONTH(DOC_DATE) ORDER BY MARGIN_HT DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);
add('quickBestMonthBasket', `SELECT TOP 5 YEAR(DOC_DATE) AS SALE_YEAR, MONTH(DOC_DATE) AS SALE_MONTH, COUNT(DISTINCT GUID) AS TICKET_COUNT, SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END) AS TOTAL_CA, CASE WHEN COUNT(DISTINCT GUID) <> 0 THEN (SUM(CASE WHEN TYPE = 0 THEN FAMOUNT ELSE FAMOUNT * -1 END) + SUM(CASE WHEN TYPE = 0 THEN VAT ELSE VAT * -1 END)) / NULLIF(COUNT(DISTINCT GUID), 0) ELSE 0 END AS AVG_BASKET FROM POS_VW_01 WHERE DOC_DATE >= @FIRST_DATE AND DOC_DATE <= @LAST_DATE AND STATUS = 1 AND DEVICE <> '9999' AND TOTAL <> 0 GROUP BY YEAR(DOC_DATE), MONTH(DOC_DATE) ORDER BY AVG_BASKET DESC`, ['FIRST_DATE:date', 'LAST_DATE:date']);

fs.writeFileSync(path, JSON.stringify(q, null, 2));
console.log('Wrote', path, Object.keys(q).length, 'entries');
